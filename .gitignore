# ── Python ─────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# ── Virtual env ─────────────────────────────────────────
.venv/
venv/

# ── Secrets ─────────────────────────────────────────────
.env
.env.*

# ── Runtime data (asla commit'e girmez) ─────────────────
data/*.db
data/*.db-*
output/*.pdf
*.sqlite
*.sqlite3

# ── OS / editor ─────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp

# Ingested donor call test artifacts
donors/ech_sudan_2026.yaml
donors/unfpa_turkiye_cefm.yaml

# LLM-Ops usage ledger
ops/usage.jsonl
donors/api_test_call.yaml
donors/multi_format_call.yaml
donors/call_mszo0ybr.yaml

# NotebookLM MCP experiment leftovers (unrelated to proposal)
nblm_*.py
donors/grant_call_for_the_prevention_of_child_early_and_forced_marriages_cefm.yaml
.hermes/
